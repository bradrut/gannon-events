import React, { Component } from 'react';

export default class EditEvent extends Component {
    render() {
        return (
            <div>
                <p>This is the page where authorized users can modify their posted events!</p>
            </div>
        )
    }
}
